import React from "react";

const Header = () => {
  return (
    <header className="bg-blue-600 ">
      <nav className="navbar">
        <a href="#">Home</a>
        <a href="#">About</a>
      </nav>
    </header>
  );
}

export default Header;
